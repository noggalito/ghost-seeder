require "spec_helper"

describe Ghost::Seeder do
  it "has a version number" do
    expect(Ghost::Seeder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
